{
  "name": "jobgenie-backend",
  "version": "1.0.0",
  "description": "This is the backend service of JobGenie web application.",
  "main": "main.ts",
  "scripts": {
    "test": "mocha", // Updated to include Mocha for testing
    "start": "ts-node src/index.ts", // Start script for running the backend
    "build": "tsc", // TypeScript build script
    "lint": "eslint 'src/**/*.{ts,tsx}'", // Linting command
    "format": "prettier --write 'src/**/*.{ts,tsx}'" // Prettier formatting
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JogGenie/backend.git"
  },
  "author": "theshdb <shdb0011@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/JogGenie/backend/issues"
  },
  "homepage": "https://github.com/JogGenie/backend#readme",
  "private": false,
  "dependencies": {
    "ioredis": "^5.2.0", // Added ioredis for Redis connection
    "eslint": "^9.10.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "express": "^4.18.2", // Express added for server setup (if needed)
    "apollo-server-express": "^3.11.1", // Apollo Server for GraphQL
    "graphql": "^16.6.0" // GraphQL dependency
  },
  "devDependencies": {
    "@types/node": "^20.4.2", // Node types
    "@types/express": "^4.17.17", // Express types
    "@types/mocha": "^10.0.1", // Mocha types for testing
    "@types/chai": "^4.3.5", // Chai types for assertions
    "@types/sinon": "^10.0.12", // Sinon types for mocks/stubs
    "chai": "^4.3.7", // Chai assertion library
    "mocha": "^10.2.0", // Mocha test runner
    "sinon": "^15.0.2", // Sinon for test spies/stubs/mocks
    "ts-node": "^10.9.1" // TypeScript Node.js integration for runtime
  }
}
